/*
hay situaciones en las que separar la lógica de las llamadas a la API en un archivo `services/api.ts` podría ser beneficioso:

Si el proyecto crece significativamente o si se prevé que la lógica de la API se volverá más compleja, separar las llamadas a la API en un archivo dedicado podría mejorar la organización y mantenibilidad del código.

*/