import { Box } from '@mui/material';
import { ConverterForm } from '../components/ConverterForm';

export const Home = () => {
  return (
    <Box sx={{ py: 4 }}>
      <ConverterForm />
    </Box>
  );
};
